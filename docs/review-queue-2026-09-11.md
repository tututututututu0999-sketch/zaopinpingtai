# 审核队列与全科检索修复

实际「25 寒假大通关 / 产品说明书2.png」的基础状态是 PENDING，视觉修订却已有 confirmed 快照，状态 WAITING_ELIGIBILITY；套系主参考已确认。原统一审核页面和接口只允许 PENDING/READY 视觉修订进入确认，导致基础审核撤回后无法恢复。

当前统一审核允许该素材重新确认基础信息，保留已经确认的视觉快照，不再次接受旧候选覆盖。事务校验修订 ID、锁定素材及修订，确认后重新排队向量任务。不会自动代替用户确认基础信息。无预览、正在处理、未识别完成的素材仍不能强制入库。

主参考入口改为当前图片摘要，展开显示图片网格；点击图卡才明确设置主参考，不顺带确认图片审核。已完成的套系不反复占据审核队列。队列保留待确认、处理中、需处理、已入库统计，每张素材说明实际阻塞原因，失败可单张重试。缺少主参考不妨碍先核对基础信息，主参考补齐后再处理向量。

语文学科检索漏掉新套系的原因是其礼盒学科字段为「全科」。检索原来只接受语文或语数英，遗漏全科/全学科别名。现将这些已知全科别名视为可用于学科检索，不改变具体学科素材之间的排除规则和向量评分。

用当前已确认语文需求 `3901b371-65a0-4269-a232-dbc9dc96c096 / 1` 在正式本地 API 只读重查，新套系「25 寒假大通关」排第一，分数 62。分数是匹配度，不是成功率。旧候选需点击「重新检索」，不会自动替换已经选定的参考。

验证覆盖已确认视觉快照的基础重新审核、错误修订拒绝、原快照保留、向量任务重新排队、全科进入语文检索而数学专科不进入；在独立测试 schema 中执行，不修改用户审核数据。另覆盖桌面/移动的图片主参考选择、选择不隐含审核确认、保存失败保留编辑内容、成功后自动移出队列。
